{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "python",
      "request": "launch",
      "name": "Debug bin",
      "program": "2.5-graph-load-taxonomy.py",
      "args": ["--load-db", "--number", "8", "--upsert"],
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
