// TODO for routing to work, we need to run this page on a subdomain so we need
// programmatic access to our DNS. should probably use aws instead of hover.

export default function AppLaunch() {
  return <></>;
}
